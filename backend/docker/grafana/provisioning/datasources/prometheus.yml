apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Fixed rather than auto-generated, so every provisioned dashboard JSON
    # can reference this uid directly and stay correct across container
    # recreations.
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
